version: '3.8'
services:
  backend:
    image: node:16-alpine
    ports:
      - "3001:3000"
    working_dir: /app
    environment:
      - NODE_ENV=production
    volumes:
      - ./backend:/app
    command: sh -c "cd /app && npm install && node server.js"
    restart: unless-stopped
